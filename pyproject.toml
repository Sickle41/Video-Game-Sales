[project]
name = "video-game-pipeline"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Sickle41", email = "dracopest@gmail.com" }
]
requires-python = ">=3.14"
dependencies = []

[project.scripts]
video-game-pipeline = "video_game_pipeline:main"

[build-system]
requires = ["uv_build>=0.11.18,<0.12.0"]
build-backend = "uv_build"
